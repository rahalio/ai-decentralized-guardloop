/**
 * ExecuteBindFairnessPolicy - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: use-cases
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteBindFairnessPolicy Use Case
 *
 * DDD: Application use case for get BindPolicy.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { BindFairnessPolicyInput, BindFairnessPolicyOutput } from "../dto/bind-policy.dto";
import type { ExecutionContextService, IdGeneratorService } from "@guardloop/services/_shared/index.js";
import type { BindPolicyRepository } from "../ports";
import { NotFoundError, ValidationError } from "../errors";

export class ExecuteBindFairnessPolicy {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly bindPolicy: BindPolicyRepository
  ) {}

  async execute(input: BindFairnessPolicyInput): Promise<BindFairnessPolicyOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.uscId();

    // All field names extracted from OpenAPI spec: appetiteLimitId, fairnessPolicyId
    // Validate input
    if (!input) {
      throw new ValidationError("Input is required");
    }
    if (!(input as any).useCaseId) {
      throw new ValidationError("useCaseId is required");
    }
    const usecaseId = (input as any).useCaseId;

    // Load entity via repository (pass correlation ID for response meta)
    const entity = await this.bindPolicy.bindFairnessPolicy({ useCaseId: usecaseId, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found: ${usecaseId}`);
    }

    return entity as BindFairnessPolicyOutput;
  }
}
