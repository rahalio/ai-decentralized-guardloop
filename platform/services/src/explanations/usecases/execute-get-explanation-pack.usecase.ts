/**
 * ExecuteGetExplanationPack - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: explanations
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteGetExplanationPack Use Case
 *
 * DDD: Application use case for get Explanation.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { GetExplanationPackInput, GetExplanationPackOutput } from "../dto/explanation.dto";
import type { ExecutionContextService, IdGeneratorService } from "@guardloop/services/_shared/index.js";
import type { ExplanationRepository } from "../ports";
import { NotFoundError, ValidationError } from "../errors";

export class ExecuteGetExplanationPack {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly explanation: ExplanationRepository
  ) {}

  async execute(input: GetExplanationPackInput): Promise<GetExplanationPackOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.expId();

    // All field names extracted from OpenAPI spec: none
    // Validate input
    if (!input) {
      throw new ValidationError("Input is required");
    }
    if (!(input as any).explanationPackId) {
      throw new ValidationError("explanationPackId is required");
    }
    const explanationpackId = (input as any).explanationPackId;

    // Load entity via repository (pass correlation ID for response meta)
    const entity = await this.explanation.getExplanationPack({ explanationPackId: explanationpackId, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found: ${explanationpackId}`);
    }

    return entity as GetExplanationPackOutput;
  }
}
