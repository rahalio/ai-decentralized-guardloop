/**
 * ExecuteOpenRemediation - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: remediations
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteOpenRemediation Use Case
 *
 * DDD: Application use case for get Remediation.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { OpenRemediationInput, OpenRemediationOutput } from "../dto/remediation.dto";
import type { ExecutionContextService, IdGeneratorService } from "@guardloop/services/_shared/index.js";
import type { RemediationRepository } from "../ports";
import { NotFoundError, ValidationError } from "../errors";

export class ExecuteOpenRemediation {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly remediation: RemediationRepository
  ) {}

  async execute(input: OpenRemediationInput): Promise<OpenRemediationOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.rmdId();

    // All field names extracted from OpenAPI spec: alertId, boardReportable, dueAt, owner, summary, useCaseId
    // Validate input
    if (!input) {
      throw new ValidationError("Input is required");
    }
    if (!(input as any).alertId) {
      throw new ValidationError("alertId is required");
    }
    const alertId = (input as any).alertId;

    // Load entity via repository (pass correlation ID for response meta)
    const entity = await this.remediation.openRemediation({ alertId: alertId, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found: ${alertId}`);
    }

    return entity as OpenRemediationOutput;
  }
}
