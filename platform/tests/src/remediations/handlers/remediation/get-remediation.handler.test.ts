/**
 * GetRemediation handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: remediations
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { getRemediation } from "@guardloop/api-server/domains/remediations/handlers";
import type { RemediationsDDDDependencies } from "@guardloop/api-server/domains/remediations";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@guardloop/tests/shared";
import { REMEDIATIONS_USE_CASES } from "../__factories__/use-cases.js";

describe("GetRemediation", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: RemediationsDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<RemediationsDDDDependencies>(
      REMEDIATIONS_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with entity when found", async () => {
    const entity = { id: 'mock-id', orgId } as any;
    const useCaseResult = { data: entity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    vi.spyOn(mockDeps.executeGetRemediation, "execute").mockResolvedValue(useCaseResult);
    mockRequest.params = { remediationId: entity.id };

    await getRemediation(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeGetRemediation.execute).toHaveBeenCalledWith(expect.objectContaining({ remediationId: entity.id }));
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

  it("should return error when entity not found", async () => {
    const nonExistentId = "non-existent-id";
    mockRequest.params = { remediationId: nonExistentId };
    
    vi.spyOn(mockDeps.executeGetRemediation, "execute").mockRejectedValue(
      new Error("Entity not found")
    );

    await expect(
      getRemediation(mockRequest, mockReply, mockDeps)
    ).rejects.toThrow();
  });

});
