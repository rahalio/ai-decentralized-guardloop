/**
 * GetFairnessPolicy handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: policies
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { getFairnessPolicy } from "@guardloop/api-server/domains/policies/handlers";
import type { PoliciesDDDDependencies } from "@guardloop/api-server/domains/policies";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@guardloop/tests/shared";
import { POLICIES_USE_CASES } from "../__factories__/use-cases.js";

describe("GetFairnessPolicy", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: PoliciesDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<PoliciesDDDDependencies>(
      POLICIES_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with entity when found", async () => {
    const entity = { id: 'mock-id', orgId } as any;
    const useCaseResult = { data: entity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    vi.spyOn(mockDeps.executeGetFairnessPolicy, "execute").mockResolvedValue(useCaseResult);
    mockRequest.params = { fairnessPolicyId: entity.id };

    await getFairnessPolicy(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeGetFairnessPolicy.execute).toHaveBeenCalledWith(expect.objectContaining({ fairnessPolicyId: entity.id }));
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

  it("should return error when entity not found", async () => {
    const nonExistentId = "non-existent-id";
    mockRequest.params = { fairnessPolicyId: nonExistentId };
    
    vi.spyOn(mockDeps.executeGetFairnessPolicy, "execute").mockRejectedValue(
      new Error("Entity not found")
    );

    await expect(
      getFairnessPolicy(mockRequest, mockReply, mockDeps)
    ).rejects.toThrow();
  });

});
