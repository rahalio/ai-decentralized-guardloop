/**
 * GetMonitorAlert handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: monitoring
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { getMonitorAlert } from "@guardloop/api-server/domains/monitoring/handlers";
import type { MonitoringDDDDependencies } from "@guardloop/api-server/domains/monitoring";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@guardloop/tests/shared";
import { MONITORING_USE_CASES } from "../__factories__/use-cases.js";

describe("GetMonitorAlert", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: MonitoringDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<MonitoringDDDDependencies>(
      MONITORING_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with entity when found", async () => {
    const entity = { id: 'mock-id', orgId } as any;
    const useCaseResult = { data: entity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    vi.spyOn(mockDeps.executeGetMonitorAlert, "execute").mockResolvedValue(useCaseResult);
    mockRequest.params = { alertId: entity.id };

    await getMonitorAlert(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeGetMonitorAlert.execute).toHaveBeenCalledWith(expect.objectContaining({ alertId: entity.id }));
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

  it("should return error when entity not found", async () => {
    const nonExistentId = "non-existent-id";
    mockRequest.params = { alertId: nonExistentId };
    
    vi.spyOn(mockDeps.executeGetMonitorAlert, "execute").mockRejectedValue(
      new Error("Entity not found")
    );

    await expect(
      getMonitorAlert(mockRequest, mockReply, mockDeps)
    ).rejects.toThrow();
  });

});
