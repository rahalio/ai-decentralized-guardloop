openapi: 3.1.0
info:
  title: Guardloop (deprecated root stub)
  version: 0.1.0
  description: |
    Deprecated. Domain OpenAPI specs live under packages/openapi-core/src/
    (use-cases, policies, assessments, controls, monitoring, remediations,
    explanations, reporting, identity). Bundle via `pnpm --filter @guardloop/openapi-core bundle`.
  license:
    name: Proprietary
    url: https://guardloop.local/license
servers:
  - url: https://api.guardloop.local
paths: {}
